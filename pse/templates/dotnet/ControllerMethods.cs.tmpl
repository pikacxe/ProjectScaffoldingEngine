    [HttpGet]
    public ActionResult<List<{{DtoType}}>> GetAll()
    {
        return Ok(new List<{{DtoType}}>());
    }

    [HttpGet("{id}")]
    public ActionResult<{{DtoType}}> GetById({{IdType}} id)
    {
        return Ok(new {{DtoType}}());
    }

    [HttpPost]
    public ActionResult<{{DtoType}}> Create({{DtoType}} request)
    {
        return CreatedAtAction(nameof(GetById), new { id = request.Id }, request);
    }
